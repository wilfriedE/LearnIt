<% if @lesson_version.errors.blank?  %>
$("#<%=@row_id%>").replaceWith("<%= escape_javascript(render 'lesson_versions/lesson_version_row', { lesson_version: @lesson_version }) %>");
<% else %>
alert("Failed to update approval");
<% end %>
