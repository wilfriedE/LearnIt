<%= form_for @lesson_version do |f| %>
  <% if @lesson_version.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_version.errors.count, "error") %> prohibited this lesson from being saved:</h2>

      <ul>
      <% @lesson_version.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
  <%= f.text_field :name, :class => "form-control" %>
  </div>

  <div class="form-group">
  <%= f.text_area :description, :class => "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit "Continue", :class => "btn btn-primary btn-raised btn-lg btn-block btn-loading"  %>
  </div>
<% end %>
