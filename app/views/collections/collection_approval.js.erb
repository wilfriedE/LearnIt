<% if @collection.errors.blank?  %>
$("#<%=@row_id%>").replaceWith("<%= escape_javascript(render 'collections/collection_row', { collection: @collection }) %>");
<% else %>
alert("Failed to update approval");
<% end %>
