<div class="card">
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p class="<%= notification.read? ? 'text-muted' : '' %>" ><%= notification.name %></p>
      <footer class="text-muted"><%= notification.description.html_safe %></footer>
    </blockquote>
  </div>
</div>
