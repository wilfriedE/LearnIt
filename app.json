{
  "name": "LearnIt",
  "description": "A self maintanable platform to help you aggregate and administer learning resources.",
  "website": "https://github.com/wilfriedE/LearnIt",
  "success_url": "/",
  "addons": ["heroku-postgresql:hobby-dev", "sendgrid:starter"],
  "env": {
    "APP_DOMAIN": {
      "description": "Your domain name. e.i  myapp_name.herokuapp.com "
    },
    "SMTP_ADDRESS": {
      "description": "Your SMTP service address e.i smtp.sendgrid.net with the sendgrid addon or smtp.gmail.com to setup gmail.",
      "value": "smtp.sendgrid.net"
    },
    "MAILER_PASSWORD": {
      "description": "SMPT service password. e.i sendgrid addon password (prefiled after deploy) or a gmail password.",
      "required": false
    },
    "MAILER_USERNAME": {
      "description": "SMPT service username. e.i sengrid addon username (prefiled after deploy) or a gmail username",
      "required": false
    },
    "MAILER_SENDER": {
      "description": "Default email address to use when sending emails. e.i sender@myapp_name.herokuapp.com"
    },
    "DEVISE_SECRET_KEY": {
      "description": "Secret key to use for Devise.",
      "generator": "secret"
    },
    "SECRET_KEY_BASE": {
      "description": "Secret key base for the app.",
      "generator": "secret"
    }
  },
  "formation": {
    "web": {
      "quantity": 1,
      "size": "free"
    }
  },
  "scripts": {
    "postdeploy": "bundle exec rake db:migrate; bundle exec rake db:seed"
  }
}
